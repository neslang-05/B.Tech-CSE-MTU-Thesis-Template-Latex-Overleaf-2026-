% ============================================================
%  CHAPTER 9: RESULTS AND EVALUATION
%  AgriVerify — AI-Powered Fake Seed Detection System
%  Manipur Technical University, Final Year Project Report
% ============================================================

\section{Model Performance}

After completing training, we rigorously tested our ResNet50 model on images it had never seen before. We measured its performance using standard metrics that the machine learning community relies on: Accuracy, Precision, Recall, and F1 Score~\cite{mohanty2016}. These metrics tell us from different angles how well the model distinguishes between genuine seeds and problematic ones (damaged or counterfeit)~\cite{he2016deep, xu2021}.

The results were exceptional. Our model reliably sorted seed samples into two categories: \texttt{Pure} (legitimate, certified seeds) and \texttt{Impure} (defective, damaged, or counterfeit seeds). These numbers confirm that AgriVerify is genuinely ready to help farmers and agricultural officers in the real world, even in areas with limited resources.

\subsection{Accuracy, Precision, Recall, and F1 Score}

Here's exactly how well our model performed on the test set:

\begin{table}[htbp]
  \centering
  \caption{Overall Model Evaluation Results}
  \label{tab:overallmetrics}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l c X @{}}
    \toprule
    \textbf{Performance Metric} & \textbf{Result} & \textbf{What This Means} \\
    \midrule
    Accuracy & 99.57\% & Out of every 100 seeds we tested, we correctly classified 99 or 100 of them \\
    \addlinespace
    Precision & 99.57\% & When the system says a seed is problematic, it's right almost every time—virtually no false alarms \\
    \addlinespace
    Recall & 99.57\% & The system catches almost all problematic seeds—very few slip through undetected \\
    \addlinespace
    F1 Score & 99.57\% & Perfect balance between precision and recall—the system is equally good at both \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.85\textwidth}
    \includegraphics[width=\textwidth]{{"thesis/model-performace.jpeg"}}
  \end{adjustbox}
  \caption{How the model improved over 15 training cycles. The top line shows accuracy getting better; the bottom line shows the error getting smaller.}
  \label{fig:model-performance}
\end{figure}

Let me explain why these numbers matter. When we achieved 99.57\% accuracy, it means the model correctly classified the vast majority of paddy seeds regardless of their texture or how the photo was lit. Precision and recall being equally high is particularly important: it means we're both good at avoiding false alarms (rejecting good seeds) and catching actual problems (approving bad seeds). A high F1 Score shows these two strengths are perfectly balanced~\cite{mohanty2016}.

Most importantly, our 99.57\% result far exceeds the industry standard of 85--90\% for automated seed quality systems~\cite{dyrmann2016, ni2009}. This validates that our approach of combining transfer learning, careful data augmentation, and specialized regularization techniques really works.

\section{Seed Quality Results: Pure vs. Impure Seeds}

To understand exactly how the model performs on each type of seed, we analyzed its performance separately for genuine seeds and problematic seeds. We tested on 231 seed samples divided into these two categories.

\begin{table}[htbp]
  \centering
  \caption{Performance on Each Seed Category}
  \label{tab:perclass}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l c c c X @{}}
    \toprule
    \textbf{Seed Type} & \textbf{Precision} & \textbf{Recall} & \textbf{F1 Score} & \textbf{What This Means} \\
    \midrule
    Pure (Genuine) & 100.0\% & 99.19\% & 99.60\% & Perfect at recognizing genuine seeds—never wrongly approves a fake one \\
    \addlinespace
    Impure (Damaged/Fake) & 99.07\% & 100.0\% & 99.53\% & Perfect at detecting problems—catches every single defective seed \\
    \bottomrule
  \end{tabularx}
\end{table}

The results show something remarkable: the model achieved 100\% precision on genuine seeds because they have very distinctive visual characteristics—a consistent golden-brown color, intact husks, and uniform texture. It also achieved 100\% recall on problematic seeds, meaning every damaged or counterfeit seed in our test set was flagged. This is exactly what we want: no genuine seeds are mistakenly rejected, and no fake seeds slip through~\cite{liu2020}.

The combination of weighted sampling (giving equal importance to each category during training) and image augmentation (showing the network varied versions of seeds) prevented the model from developing a bias toward whichever category was more common in the data.

\section{Confusion Matrix Analysis}

To see the exact pattern of the model's decisions, we created a confusion matrix—a table showing which predictions were correct and which were wrong.

\begin{table}[htbp]
  \centering
  \caption{Detailed Look at Every Prediction}
  \label{tab:confusion}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{0.75\textwidth}{@{} l c c @{}}
    \toprule
    \multirow{2}{*}{\textbf{Actual Seed Type}} & \multicolumn{2}{c}{\textbf{What the Model Predicted}} \\
    \cmidrule{2-3}
    & \textbf{Impure} & \textbf{Pure} \\
    \midrule
    \textbf{Impure} & 107 & 0 \\
    \addlinespace
    \textbf{Pure} & 1 & 123 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.75\textwidth}
    \includegraphics[width=\textwidth]{{"thesis/Confusion-Matrix.jpeg"}}
  \end{adjustbox}
  \caption{Visual representation of how often the model got predictions right or wrong.}
  \label{fig:confusion-matrix}
\end{figure}

Out of 231 test seeds, 230 were classified correctly—that's almost perfect. Let's look at the two types of mistakes:

**Out of 107 problematic seeds:** All 107 were correctly identified as impure. Zero problematic seeds were falsely approved. This is crucial—it means no farmer will unknowingly plant counterfeit or damaged seeds because the system missed them.

**Out of 124 genuine seeds:** 123 were correctly approved, and only 1 was conservatively rejected (marked as impure when it was actually genuine). This conservative error is actually good for agricultural safety: if a genuine seed batch gets flagged, the farmer can request manual re-inspection. But if a fake batch gets approved and the farmer plants it, the entire harvest could fail. So falsely rejecting a good seed is a much safer mistake than falsely approving a bad one.

\section{Regularization Effectiveness}

We only had 1,563 training images, which is relatively small for deep learning. This means the model could easily "memorize" the training data instead of learning general patterns. To prevent this overfitting, we used multiple protective techniques. Let's see how effective they were:

\begin{table}[htbp]
  \centering
  \caption{How Our Anti-Overfitting Techniques Helped}
  \label{tab:regularization}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l c X @{}}
    \toprule
    \textbf{Configuration} & \textbf{Training vs. Validation Difference} & \textbf{What Happened} \\
    \midrule
    No Protection & 25--30\% & Bad—the model memorized training data and performed much worse on new seeds \\
    \addlinespace
    Dropout Only & 8--12\% & Better, but still some overfitting on tricky seed textures \\
    \addlinespace
    All Techniques Combined & 2.8\% & Excellent—tight control with less than 5\% difference \\
    \bottomrule
  \end{tabularx}
\end{table}

The final gap of just 2.8\% between training and validation performance is excellent. It means the model learned general seed characteristics rather than memorizing specific training examples. We achieved this tight control using several coordinated techniques:

\begin{itemize}
    \item \textbf{Smart Weight Decay (AdamW):} We gently penalized large weights, keeping the model simple and generalizable~\cite{loshchilov2019adamw}.
    
    \item \textbf{Label Smoothing:} Instead of forcing the model to be 100\% confident about labels, we softened the targets slightly, preventing overconfidence~\cite{muller2019label}.
    
    \item \textbf{Dropout and Batch Normalization:} During training, we randomly "turn off" 50\% of neurons and normalized values to stabilize learning~\cite{he2016deep}.
    
    \item \textbf{Adaptive Learning Rate Scheduling:} We adjusted the learning rate dynamically, allowing the network to escape local traps and find better solutions.
    
    \item \textbf{Aggressive Data Augmentation:} We artificially created new training examples by rotating, flipping, blurring, and color-shifting seed images to simulate real-world field photography variations.
\end{itemize}

\section{Model Comparison and Architecture Selection}

Before settling on ResNet50, we tested four popular neural network designs on the same dataset with the same training approach to see which was best.

\begin{table}[htbp]
  \centering
  \caption{Comparing Four Different Neural Network Architectures}
  \label{tab:modelcomparison}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} X c c c c c @{}}
    \toprule
    \textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1 Score} & \textbf{Training Time} \\
    \midrule
    \textbf{ResNet50} & \textbf{99.57\%} & \textbf{99.57\%} & \textbf{99.57\%} & \textbf{99.57\%} & 16.4 min \\
    \addlinespace
    \textbf{DenseNet121} & 99.57\% & 99.57\% & 99.57\% & \textbf{99.57\%} & 15.0 min \\
    \addlinespace
    \textbf{GoogleNet} & 99.57\% & 99.57\% & 99.57\% & \textbf{99.57\%} & 12.2 min \\
    \addlinespace
    \textbf{MobileNetV2} & 99.13\% & 99.15\% & 99.13\% & 99.13\% & \textbf{8.5 min} \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.85\textwidth}
    \includegraphics[width=\textwidth]{{"thesis/model-comparison.jpeg"}}
  \end{adjustbox}
  \caption{Visual comparison of how each model performed and how long training took.}
  \label{fig:model-comparison}
\end{figure}

Three models tied for best accuracy—ResNet50, DenseNet121, and GoogleNet all achieved 99.57\%. MobileNetV2 was slightly lower at 99.13\% but trained much faster. So why did we choose ResNet50?

ResNet50 has skip connections (shortcuts) that let training signals flow smoothly through all 50 layers, making it easier to fine-tune without losing information~\cite{he2016deep, xu2021}. More importantly, ResNet50 is the most widely supported across different deployment platforms—PyTorch, TorchScript, ONNX, and specialized inference engines. This flexibility means AgriVerify can run on cloud servers, edge devices, and even smartphones in the future. The extra 4.4 minutes of training time is negligible since training only happens once~\cite{pytorch2019}.

\section{System Performance and Production Latency}

Beyond just accuracy, we tested how fast the complete system responds to real requests—this is critical for farmers waiting for results in the field.

\subsection{How Fast the AI Model Runs}

Inference latency is the time it takes the model to receive an image, process it, and output a prediction.

\begin{table}[htbp]
  \centering
  \caption{Speed on Different Hardware}
  \label{tab:latency}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l c X @{}}
    \toprule
    \textbf{Hardware} & \textbf{Speed} & \textbf{Good For What?} \\
    \midrule
    Regular CPU & $\sim$165 ms & Lightweight servers and edge devices; still plenty fast \\
    \addlinespace
    GPU (NVIDIA CUDA) & $\sim$20 ms & Cloud servers handling many requests; 8 times faster than CPU \\
    \bottomrule
  \end{tabularx}
\end{table}

Even on a regular CPU (without GPU acceleration), the model responds in 165 milliseconds—that's less than a quarter-second. On GPU hardware, it's 20 milliseconds. Both are fast enough for real-time farmer feedback in the field.

\subsection{Full System Response Time}

We stress-tested the entire backend system (not just the model) to see how it performs when many farmers are using it simultaneously.

\begin{table}[htbp]
  \centering
  \caption{Real-World System Performance Under Load}
  \label{tab:backendperformance}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l c X @{}}
    \toprule
    \textbf{Metric} & \textbf{Observation} & \textbf{Significance} \\
    \midrule
    End-to-End Response & 280--420 ms & Includes uploading the image, sending it through the system, running AI, and formatting the response \\
    \addlinespace
    Concurrent Requests & 40--60 per second & Very stable even when many farmers submit seeds simultaneously \\
    \addlinespace
    Image Upload \& Prep & 80--120 ms & Network transfer and image preprocessing before AI runs \\
    \addlinespace
    Database Queries & $<$50 ms & Fast retrieval of complaint and user records \\
    \bottomrule
  \end{tabularx}
\end{table}

The system remained stable and responsive even under peak load. The proxy architecture (where the frontend acts as an intermediary) was crucial—it prevented the backend from getting overwhelmed because it manages heavy workloads asynchronously instead of forcing everything to wait~\cite{martin2018clean}.

\section{Functional Verification and Test Execution}

We rigorously tested every major feature to confirm the system works correctly, is secure, and maintains data consistency.

\subsection{Seed Verification Testing}

We tested the image processing and classification system with many different scenarios:

\begin{table}[htbp]
  \centering
  \caption{Seed Verification Test Cases}
  \label{tab:seedtestcases}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l X X c @{}}
    \toprule
    \textbf{Test ID} & \textbf{What We Tested} & \textbf{Expected Behavior} & \textbf{Result} \\
    \midrule
    \textbf{SV-01} & Upload clear photos of genuine seed packaging & Correctly classified as Pure with physical measurements extracted & \textbf{Pass} \\
    \addlinespace
    \textbf{SV-02} & Upload photos of cracked or discolored seeds & Classified as Impure (Damaged) with warnings shown & \textbf{Pass} \\
    \addlinespace
    \textbf{SV-03} & Upload fake packaging with counterfeit branding & Classified as Impure (Fake) with high confidence & \textbf{Pass} \\
    \addlinespace
    \textbf{SV-04} & Try uploading wrong file types (.exe, .zip, .txt) & System rejects and shows helpful error message & \textbf{Pass} \\
    \addlinespace
    \textbf{SV-05} & Upload blurry or very dark seed photos & System warns user to retake photo or shows low confidence & \textbf{Pass} \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Grievance Management Testing}

We tested the complaint system to make sure complaints are recorded securely and can be tracked:

\begin{table}[htbp]
  \centering
  \caption{Complaint System Test Cases}
  \label{tab:complainttestcases}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l X X c @{}}
    \toprule
    \textbf{Test ID} & \textbf{What We Tested} & \textbf{Expected Behavior} & \textbf{Result} \\
    \midrule
    \textbf{CM-01} & Farmer files a complaint with description and evidence & Complaint saved to database and marked as Open & \textbf{Pass} \\
    \addlinespace
    \textbf{CM-02} & Try filing complaint with missing required information & Form validation prevents submission and shows error & \textbf{Pass} \\
    \addlinespace
    \textbf{CM-03} & Farmer views their complaint history & All past and current complaints displayed with pagination & \textbf{Pass} \\
    \addlinespace
    \textbf{CM-04} & Officer changes complaint status from Open to Resolved & Update immediately visible in the system & \textbf{Pass} \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Security and Access Control Testing}

We tested that only authorized users can access their roles' features:

\begin{table}[htbp]
  \centering
  \caption{Security and Access Control Test Cases}
  \label{tab:rbactestcases}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabularx}{\textwidth}{@{} l X X c @{}}
    \toprule
    \textbf{Test ID} & \textbf{What We Tested} & \textbf{Expected Behavior} & \textbf{Result} \\
    \midrule
    \textbf{RB-01} & Valid farmer login & Receives security token and goes to farmer dashboard & \textbf{Pass} \\
    \addlinespace
    \textbf{RB-02} & Farmer tries accessing admin page & System blocks access and redirects to farmer area & \textbf{Pass} \\
    \addlinespace
    \textbf{RB-03} & Admin login with multi-factor authentication & Full admin privileges granted & \textbf{Pass} \\
    \addlinespace
    \textbf{RB-04} & Security token expires during session & System automatically gets new token without user noticing & \textbf{Pass} \\
    \addlinespace
    \textbf{RB-05} & Login with wrong password & System returns error and shows helpful message & \textbf{Pass} \\
    \bottomrule
  \end{tabularx}
\end{table}

% ──────────────────────────────────────────────────────────────
\section{System User Interfaces and Operational Workflows}
\label{sec:system-interfaces}
% ──────────────────────────────────────────────────────────────

AgriVerify provides simple, intuitive interfaces tailored to three different user types: farmers, agricultural officers, and system administrators. We built these using modern web technologies: Next.js 15, React 19, and Tailwind CSS~\cite{nextjs2024, react2013, tailwind2024}. Let's walk through each interface and see how users interact with the system.

\subsection{Public Portal, Onboarding, and Multilingual Support}

When farmers first visit AgriVerify, they land on a public page explaining what the platform does and why they should use it. Since many rural farmers speak regional languages, the interface supports language selection—farmers can switch between English and their local dialect.

\begin{figure}[htbp]
  \centering
  \subfloat[Main landing page showing AgriVerify's value and features.]{\includegraphics[width=0.48\textwidth]{{"thesis/Landing.png"}}\label{fig:ui-landing}}
  \hfill
  \subfloat[Language selection interface so rural farmers can use their preferred language.]{\includegraphics[width=0.48\textwidth]{{"thesis/language.png"}}\label{fig:ui-language}}
  \caption{Welcome pages for onboarding and language selection.}
  \label{fig:ui-public-portal}
\end{figure}

New farmers can create accounts by providing their name, email, location, and agricultural district. This information is essential for the complaint system—it verifies that users are real people in the agricultural community.

\begin{figure}[htbp]
  \centering
  \subfloat[Login and role selection interface.]{\includegraphics[width=0.48\textwidth]{{"thesis/login landing.png"}}\label{fig:ui-login-landing}}
  \hfill
  \subfloat[Farmer registration form collecting demographic and location information.]{\includegraphics[width=0.48\textwidth]{{"thesis/new account.png"}}\label{fig:ui-new-account}}
  \caption{Login and farmer account registration.}
  \label{fig:ui-onboarding}
\end{figure}

For agricultural officers and system administrators, there's a separate secure login page that requires both a password and a special time-based security code (multi-factor authentication) to prevent unauthorized access.

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.75\textwidth}
    \includegraphics[width=\textwidth]{{"thesis/admin login.png"}}
  \end{adjustbox}
  \caption{Secure admin login requiring multi-factor authentication.}
  \label{fig:ui-admin-login}
\end{figure}

\subsection{Farmer Portal and AI Seed Quality Assessment Workflow}

Once logged in, farmers see a personalized dashboard showing their verification history, verification statistics, and any alerts. This is their command center for seed quality checking.

\begin{figure}[htbp]
  \centering
  \subfloat[Main farmer dashboard showing verification history and activity summary.]{\includegraphics[width=0.48\textwidth]{{"thesis/dashboard.png"}}\label{fig:ui-dashboard}}
  \hfill
  \subfloat[Farmer profile page showing account information and verification badges.]{\includegraphics[width=0.48\textwidth]{{"thesis/profile.png"}}\label{fig:ui-profile}}
  \caption{Farmer dashboard and profile management.}
  \label{fig:ui-farmer-portal}
\end{figure}

Farmers can update their profile information at any time. The core feature is submitting seed photos—farmers take pictures of the front and back of seed packaging using their phone or computer camera.

\begin{figure}[htbp]
  \centering
  \subfloat[Profile editing interface.]{\includegraphics[width=0.48\textwidth]{{"thesis/rename.png"}}\label{fig:ui-rename}}
  \hfill
  \subfloat[Mobile-friendly camera interface for uploading front and back seed packaging photos.]{\includegraphics[width=0.48\textwidth]{{"thesis/adding seed image.png"}}\label{fig:ui-adding-seed}}
  \caption{Account settings and seed photo submission.}
  \label{fig:ui-capture-workflow}
\end{figure}

After submitting photos, the system analyzes them and returns a detailed report. If the seeds are genuine, the report shows the extracted physical measurements. If the system detects a problem, it displays a warning with clear instructions on what to do next.

\begin{figure}[htbp]
  \centering
  \subfloat[Verification result for genuine seeds showing extracted physical parameters.]{\includegraphics[width=0.48\textwidth]{{"thesis/geniune.png"}}\label{fig:ui-genuine}}
  \hfill
  \subfloat[Alert for counterfeit seeds with confidence breakdown and reporting instructions.]{\includegraphics[width=0.48\textwidth]{{"thesis/counterfeit.png"}}\label{fig:ui-counterfeit}}
  \caption{Seed quality assessment results.}
  \label{fig:ui-verification-results}
\end{figure}

\subsection{Grievance Redressal and Officer Investigation Interfaces}

If a farmer encounters fake or damaged seeds, they can file a formal complaint. The complaint form asks for a description of the problem, the seed batch number, and a severity rating.

\begin{figure}[htbp]
  \centering
  \subfloat[Complaint form where farmers describe the problem and adjust severity.]{\includegraphics[width=0.48\textwidth]{{"thesis/complains.png"}}\label{fig:ui-complaints-form}}
  \hfill
  \subfloat[Farmer's complaint tracking page showing status updates.]{\includegraphics[width=0.48\textwidth]{{"thesis/complains queue.png"}}\label{fig:ui-complaints-queue}}
  \caption{Complaint filing and tracking for farmers.}
  \label{fig:ui-grievance}
\end{figure}

Agricultural officers have a separate dashboard showing all complaints in their region. They can filter by severity and see which areas have the most problems. This helps them prioritize investigations.

\begin{figure}[htbp]
  \centering
  \subfloat[Officer dashboard showing regional complaint summary and workload metrics.]{\includegraphics[width=0.48\textwidth]{{"thesis/officer dashboard.png"}}\label{fig:ui-officer-dashboard}}
  \hfill
  \subfloat[Investigation queue that officers can filter and sort by severity and location.]{\includegraphics[width=0.48\textwidth]{{"thesis/investication queue.png"}}\label{fig:ui-investigation-queue}}
  \caption{Agricultural officer portal and complaint queue.}
  \label{fig:ui-officer-portal}
\end{figure}

When officers click on a specific complaint, they can see the farmer's photos, read their description, and update the investigation status.

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.85\textwidth}
    \includegraphics[width=\textwidth]{{"thesis/complains officer.png"}}
  \end{adjustbox}
  \caption{Officer view of a complaint with farmer evidence and status update options.}
  \label{fig:ui-complaint-officer}
\end{figure}

\subsection{Platform Administration, User Governance, and Telemetry}

System administrators have a complete dashboard showing real-time system health: response times, active users, and database performance. This allows them to spot problems quickly.

\begin{figure}[htbp]
  \centering
  \subfloat[Admin telemetry dashboard showing system performance metrics in real time.]{\includegraphics[width=0.48\textwidth]{{"thesis/Admin Dashboard.png"}}\label{fig:ui-admin-dashboard}}
  \hfill
  \subfloat[User management interface for creating officers and monitoring farmer accounts.]{\includegraphics[width=0.48\textwidth]{{"thesis/user management.png"}}\label{fig:ui-user-mgmt}}
  \caption{Administrator dashboard and user management.}
  \label{fig:ui-admin-governance}
\end{figure}

The platform also provides detailed analytics showing verification trends over time and which regions have the most fake seed problems. This information helps agricultural policymakers identify where counterfeit operations are concentrated.

\begin{figure}[htbp]
  \centering
  \subfloat[Analytics dashboard showing verification trends and fake seed detection rates over time.]{\includegraphics[width=0.48\textwidth]{{"thesis/reports and analytics.png"}}\label{fig:ui-reports-1}}
  \hfill
  \subfloat[Regional analysis showing which districts have the most counterfeit seed problems.]{\includegraphics[width=0.48\textwidth]{{"thesis/reports and analytics 2nd.png"}}\label{fig:ui-reports-2}}
  \caption{System analytics and reporting dashboards.}
  \label{fig:ui-analytics}
\end{figure}

% ──────────────────────────────────────────────────────────────
\section{Challenges Encountered and Mitigation Strategies}
\label{sec:challenges}
% ──────────────────────────────────────────────────────────────

Building AgriVerify wasn't without obstacles. Here are the main challenges we faced and how we solved them:

\begin{itemize}
    \item \textbf{Limited Training Data:} We only had 1,563 seed images, which is small for deep learning. The risk was that the model would memorize training examples instead of learning general patterns. Solution: We used pre-trained ImageNet weights as a starting point, applied heavy data augmentation (creating artificial variations of existing images), and used weight decay to keep the model simple~\cite{krizhevsky2012imagenet, loshchilov2019adamw}.
    
    \item \textbf{Similar-Looking Defects:} Mechanically damaged seeds and sophisticated counterfeits can look similar in photographs. Solution: We combined visual analysis with text extraction from packaging (OCR), giving the model multiple sources of information to distinguish between damage types~\cite{mohanty2016}.
    
    \item \textbf{Varying Lighting in the Field:} Farmers use basic mobile phone cameras in unpredictable sunlight, causing images to be bright, dim, or unevenly lit. The model could struggle with these variations. Solution: We added automatic image adjustment in our frontend system (correcting orientation and contrast) and trained with color shifting augmentation to prepare the model for lighting variations.
    
    \item \textbf{Large Model File Size:} ResNet50 with all its parameters takes about 100 MB of storage. For a lightweight cloud service, this is significant. Solution: We used PyTorch's compression techniques and half-precision floating-point numbers (FP16), which reduced file size without noticeably hurting accuracy~\cite{pytorch2019}.
\end{itemize}

% ──────────────────────────────────────────────────────────────
\section{Summary and System Limitations}
\label{sec:limitations}
% ──────────────────────────────────────────────────────────────

In this chapter, we comprehensively evaluated AgriVerify across every dimension: model accuracy, system responsiveness, and user workflows. Our ResNet50 model achieved 99.57\% accuracy and an F1 Score of 99.57\%, demonstrating exceptional performance in distinguishing genuine seeds from damaged or counterfeit ones. The backend system proved stable and responsive even under simulated peak traffic. All functional tests passed, confirming that farmers, officers, and administrators can reliably use the platform.

However, several limitations remain that represent opportunities for future work:

\begin{itemize}
    \item \textbf{Single Crop Focus:} The model is trained only on paddy seeds. Supporting other crops like wheat, maize, or soybeans would require collecting and training on images of those crops~\cite{xu2021}.
    
    \item \textbf{Cloud Dependency:} The system currently requires internet connectivity to reach the cloud AI service. Offline classification on smartphones using quantized models would make AgriVerify accessible to farmers in areas with poor connectivity.
    
    \item \textbf{Disease Detection Not Included:} The current system grades overall seed quality but doesn't detect specific plant diseases. Adding phytopathological disease detection would provide even richer agricultural insights~\cite{mohanty2016}.
\end{itemize}

These limitations open promising directions for future research: deploying quantized models on mobile devices for offline use, expanding to multiple crops, and integrating explainable AI visualizations (heatmaps showing what the model "sees") to build farmer trust.