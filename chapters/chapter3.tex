% =========================================================================
% CHAPTER 3: SYSTEM DESIGN AND ARCHITECTURE
% =========================================================================
This chapter details the design of the proposed system. It includes the block diagram, system architecture, database schema, and mathematical formulation of the models used.

\section{System Architecture}
Describe the high-level architecture of your system (e.g., Client-Server architecture, Microservices, or MVC pattern). It is helpful to visualize the flow using block diagrams.

Figure~\ref{fig:architecture-placeholder} shows an example of how you can include a figure block in LaTeX. Make sure to place your diagrams inside the \texttt{images/} folder.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{assets/logo.png}
    \caption{Placeholder Logo: Replace this with your System Architecture Diagram}
    \label{fig:architecture-placeholder}
\end{figure}

\section{Proposed Methodology}
Detail the steps involved in your algorithm or system pipeline. You can use numbered lists or flowchart references:
\begin{enumerate}
    \item \textbf{Data Acquisition:} Gathering raw data from sensors, users, or external APIs.
    \item \textbf{Preprocessing:} Cleaning, normalising, and transforming the input data.
    \item \textbf{Processing:} Applying core models, neural networks, or processing scripts.
    \item \textbf{Output Generation:} Presenting the results, saving to database, or returning API responses.
\end{enumerate}

\section{Mathematical Model}
Describe any mathematical formulas or loss functions used in your project. In LaTeX, you can write inline equations like $E = mc^2$ or block equations.

An example of a labeled block equation for reference:
\begin{equation}
    \text{Softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{C} e^{z_j}}
    \label{eq:softmax}
\end{equation}
Equation~\ref{eq:softmax} represents the Softmax activation function used in the final layer of our classification neural network to compute probabilities over $C$ target classes.

Another example using the \texttt{align} environment:
\begin{align}
    \text{MSE} &= \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2 \\
    \text{MAE} &= \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|
\end{align}
These metrics are commonly used in regression models to calculate prediction errors.
