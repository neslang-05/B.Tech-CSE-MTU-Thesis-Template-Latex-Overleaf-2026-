% ============================================================
% CHAPTER 5: MODEL SERVING API
% ============================================================

% \chapter{Model Serving API}
% \label{ch:model-serving-api}

Translating offline trained machine learning models into operational real-time services requires robust, low-latency inference infrastructure. While model development prioritizes architectural optimization and empirical accuracy, the deployment tier dictates real-world responsiveness, concurrent throughput, and operational resilience \cite{martin2017clean}. Within the AgriVerify architecture, the PyTorch ResNet-50 classification model (Chapter 4) is encapsulated within a dedicated, high-performance microservice. 

This chapter details the architectural design of the Model Serving microservice, engineered using FastAPI and Uvicorn \cite{ramirez2023fastapi}. It examines the asynchronous request lifecycle, the rigorous tensor normalization pipeline, explicit API contractual specifications, and containerised deployment strategies using Docker multi-stage builds on Google Cloud Platform (GCP) Cloud Run \cite{merkel2014docker}.

% ──────────────────────────────────────────────────────────────
\section{API Architecture and Request Orchestration}
\label{sec:api-architecture-overview}

\subsection{FastAPI and ASGI Serving Stack}
The inference microservice utilizes the FastAPI framework running atop the Uvicorn Asynchronous Server Gateway Interface (ASGI) runtime. FastAPI was selected for its exceptional asynchronous I/O concurrency, built-in Pydantic type validation, and automated OpenAPI schema generation \cite{ramirez2023fastapi}. Uvicorn provides a highly optimized event loop (via \texttt{uvloop}), enabling the handling of thousands of concurrent network connections with minimal memory overhead.

The stack strictly decouples network deserialization, Pydantic validation, OpenCV preprocessing, and PyTorch tensor execution. This isolation ensures computationally intensive deep learning operations execute in dedicated thread pools without blocking the ASGI web server event loop.

\subsection{End-to-End Model Request Flow}
As illustrated in Figure~\ref{fig:model_request_flow}, an inference request begins when the ASP.NET Core backend issues a multipart POST request containing a seed image. FastAPI validates the MIME constraints and payload boundaries. The validated byte stream is decoded in-memory into an OpenCV matrix. 

Parallel execution pathways process the matrix: a PyTorch inference engine (pre-loaded with frozen ResNet-50 weights to eliminate cold-start latency) computes classification logits \cite{paszke2019pytorch, he2016deep}, while OpenCV contour algorithms extract 17 Distinctness, Uniformity, and Stability (DUS) physical parameters \cite{bradski2000opencv}. The results are aggregated, scored against reference cultivars, serialized into JSON, and returned.

\begin{figure}[H]
  \centering
  \begin{adjustbox}{max width=\textwidth}
    \begin{tikzpicture}[
      node distance=0.6cm and 0.8cm,
      box/.style={draw=black!80, fill=blue!5, thick, rounded corners=4pt, align=center, font=\scriptsize, minimum width=2.8cm, minimum height=1cm},
      subbox/.style={draw=black!80, fill=orange!10, thick, rounded corners=4pt, align=center, font=\scriptsize, minimum width=3.2cm, minimum height=0.9cm},
      arrow/.style={->, thick, >=stealth, draw=black!80}
    ]
      % Nodes
      \node[box, fill=gray!10] (req) {1. Image Payload\\(Multipart POST)};
      \node[box, fill=cyan!10, right=of req] (asgi) {2. ASGI Validation\\(FastAPI \& Pydantic)};
      \node[box, fill=purple!10, right=of asgi] (pre) {3. Preprocessing\\(OpenCV \& Tensor)};
      
      \node[subbox, right=of pre, yshift=0.8cm] (resnet) {4a. PyTorch Engine\\(ResNet-50 Inference)};
      \node[subbox, right=of pre, yshift=-0.8cm, fill=teal!10] (dus) {4b. Morphology\\(OpenCV DUS Metrics)};
      
      \node[box, fill=green!10, right=of resnet, yshift=-0.8cm] (post) {5. Aggregation \&\\JSON Serialization};

      % Background AI box
      \begin{scope}[on background layer]
        \node[draw=orange!60, fill=orange!5, dashed, rounded corners=6pt, inner sep=10pt, fit=(resnet) (dus)] (inf_bg) {};
        \node[anchor=south west, font=\tiny\bfseries\color{orange!80!black}, yshift=2pt] at (inf_bg.north west) {Concurrent AI Execution Engine};
      \end{scope}

      % Arrows
      \draw[arrow] (req) -- (asgi);
      \draw[arrow] (asgi) -- (pre);
      \draw[arrow] (pre) |- (resnet);
      \draw[arrow] (pre) |- (dus);
      \draw[arrow] (resnet) -| (post);
      \draw[arrow] (dus) -| (post);
    \end{tikzpicture}
  \end{adjustbox}
  \caption{Asynchronous model request processing flow. Tensor classification and OpenCV feature extraction execute concurrently to minimize latency.}
  \label{fig:model_request_flow}
\end{figure}

% ──────────────────────────────────────────────────────────────
\section{Inference Execution and Preprocessing Pipeline}
\label{sec:inference-pipeline}

\subsection{Tensor Normalization Mathematics}
Deep neural networks are highly sensitive to input domain shifts. To guarantee alignment with the training domain, standard image transformations are applied. Incoming BGR byte buffers are converted to RGB, resized to $224 \times 224$ pixels via bicubic interpolation, and scaled to a $[0.0, 1.0]$ float interval. Channel-wise standardization is applied using ImageNet moments \cite{paszke2019pytorch}:
\begin{equation}
    \mathbf{X}_{\text{norm}} = \frac{\mathbf{X} - \boldsymbol{\mu}}{\boldsymbol{\sigma}}
\end{equation}
where $\boldsymbol{\mu} = [0.485, 0.456, 0.406]$ and $\boldsymbol{\sigma} = [0.229, 0.224, 0.225]$. The tensor is expanded to batch shape $(1, 3, 224, 224)$.

\subsection{Confidence Scoring and Latency Telemetry}
Following the forward pass, unnormalized logits $\mathbf{z} = [z_1, z_2, z_3]$ are mapped to class probabilities using a softmax activation:
\begin{equation}
    P(y = i \mid \mathbf{z}) = \frac{e^{z_i / T}}{\sum_{j=1}^{3} e^{z_j / T}}
\end{equation}
where $T=1.0$. To prevent overconfident misclassifications on out-of-distribution imagery, an uncertainty threshold is enforced: if the top-1 probability $P(y=i^*) < 0.75$, the sample is flagged as \texttt{Suspicious}. Furthermore, wall-clock execution latency is captured via monotonic timers (\texttt{time.perf\_counter()}) and embedded directly into the response payload as \texttt{inference\_ms} for upstream telemetry tracking.

% ──────────────────────────────────────────────────────────────
\section{API Specification and Endpoint Contracts}
\label{sec:api-specification}

The microservice exposes a tightly constrained RESTful API. Tables~\ref{tab:api_spec_predict} and \ref{tab:api_spec_health} outline the schema contracts, ensuring robust typed communication with the backend orchestrator.

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Contractual Specification for the \texttt{POST /predict} Endpoint}
  \label{tab:api_spec_predict}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{@{} l X @{}}
    \toprule
    \textbf{Attribute} & \textbf{Endpoint Constraint / Schema Definition} \\
    \midrule
    \textbf{Route \& Auth} & \texttt{POST /predict} \textbar{} Requires \texttt{Authorization: Bearer <token>} \\
    \textbf{Payload} & \texttt{multipart/form-data} (File: \texttt{image}). Types: JPEG/PNG. Max size: 10MB. \\
    \textbf{200 OK} & \texttt{\{"predicted\_label": "Genuine", "confidence": 0.98, "inference\_ms": 42.8,}\\
    \textbf{(Response)} & \texttt{ "dus\_parameters": \{"length\_mm": 8.4, "aspect\_ratio": 3.1\}\}} \\
    \textbf{Error Codes} & \textbf{400}: Malformed payload \textbar{} \textbf{413}: File too large \textbar{} \textbf{415}: Invalid MIME type \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Contractual Specification for the \texttt{GET /health} Operational Endpoint}
  \label{tab:api_spec_health}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{@{} l X @{}}
    \toprule
    \textbf{Attribute} & \textbf{Endpoint Constraint / Schema Definition} \\
    \midrule
    \textbf{Route} & \texttt{GET /health} (Unauthenticated liveness/readiness probe) \\
    \textbf{Probing Logic} & Confirms Uvicorn ASGI event loop and verifies ResNet-50 weights memory load. \\
    \textbf{200 OK} & \texttt{\{"status": "online", "model\_loaded": true, "device": "cuda:0"\}} \\
    \textbf{503 Error} & Service unavailable (model weights corrupted or GPU allocation failure). \\
    \bottomrule
  \end{tabularx}
\end{table}

% ──────────────────────────────────────────────────────────────
\section{Containerisation and Deployment Architecture}
\label{sec:containerization}

To ensure perfect reproducibility while maintaining a minimal footprint, the service utilizes a multi-stage Docker build pipeline \cite{merkel2014docker}. The \textbf{Builder Stage} compiles large Python binaries (\texttt{.whl}) utilizing \texttt{gcc}. The \textbf{Runtime Stage} pulls these pre-compiled binaries into an ultra-slim, security-hardened Debian image (\texttt{python:3.10-slim}), stripping compilation toolchains. This decouples build logic from execution, compressing the final image from $>3$GB to $\sim$850MB and reducing the vulnerability attack surface.

\begin{figure}[H]
  \centering
  \begin{adjustbox}{max width=\textwidth}
    \begin{tikzpicture}[
      node distance=0.8cm and 1cm,
      box/.style={draw=black!80, fill=blue!5, thick, rounded corners=4pt, align=center, font=\scriptsize, minimum width=3cm, minimum height=1cm},
      arrow/.style={->, thick, >=stealth, draw=black!80}
    ]
      \node[box] (git) {1. CI/CD Pipeline\\(GitHub Actions)};
      \node[box, fill=purple!10, right=of git] (docker) {2. Multi-Stage Build\\(Docker Builder $\rightarrow$ Slim)};
      \node[box, fill=cyan!10, right=of docker] (registry) {3. Artifact Registry\\(Version Tagging)};
      \node[box, fill=green!10, right=of registry] (run) {4. GCP Cloud Run\\(Serverless Auto-scale)};

      \draw[arrow] (git) -- (docker);
      \draw[arrow] (docker) -- (registry);
      \draw[arrow] (registry) -- (run);
    \end{tikzpicture}
  \end{adjustbox}
  \caption{Automated CI/CD pipeline and Serverless GCP Deployment Architecture.}
  \label{fig:container_deployment_workflow}
\end{figure}

The image is deployed to Google Cloud Platform (GCP) Cloud Run, a fully managed serverless environment \cite{biswas2022gcp}. Cloud Run natively handles HTTPS termination, dynamic load balancing, and scales to zero during idle periods to optimize infrastructure costs.

% ──────────────────────────────────────────────────────────────
\section{Configuration and Defensive Security Hardening}
\label{sec:configuration-management}

Adhering to the 12-Factor application methodology \cite{wiggins2011twelve}, all configuration overrides (e.g., \texttt{ALLOWED\_CORS\_ORIGINS}, \texttt{MAX\_IMAGE\_SIZE\_MB}) and cryptographic secrets (\texttt{API\_KEY\_SECRET}) are injected securely at runtime via GCP Secret Manager, isolating credentials from version control.

To defend against malicious payloads, input sanitization is executed at the ASGI middleware level. \texttt{Content-Length} anomalies trigger connection termination before memory allocation, preventing Denial of Service (DoS) attacks via payload bombs. Strict TLS~1.3 encryption prevents data-in-transit eavesdropping, while global exception handlers sanitize HTTP error responses to suppress internal Python stack traces from potential attackers.

% ──────────────────────────────────────────────────────────────
\section{Summary}
\label{sec:chapter5-summary}

This chapter detailed the engineering architecture of the AgriVerify Model Serving microservice. By synthesizing FastAPI, PyTorch, and OpenCV within an asynchronous execution loop, the service achieves high concurrent throughput for real-time seed classification. The implementation of strict Pydantic API contracts, multi-stage Docker containerisation, and serverless GCP deployment establishes a resilient, publication-ready AI inference engine capable of supporting large-scale agricultural verification operations.